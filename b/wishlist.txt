1.
	#index .staff_pick
	&
	#index #dailygazelle

	imgs to have their own containers


2.
	#collections .hovereffect .collection_art

	to have images being applied through HTML 'img' instead of CSS 'background-image'

3.
	#index #markread

	to be beside .head instead of being inside

4.
	#forums .forum_index .forum_index tr[class^="row"] >td:nth-child(3) .last_topic a

	text legth limit to be managed with 'text-overflow' instead of being predetermined

5.
	#torrents .ratings
	score to be combined with " / 10 "(alternatively " / 10 " to be contained in a separate span as well) and be apart from the reviewer icon (instead of being contained inside it)

6.
	#torrents  .box_tags
	"There are no tags to display." message to have a container